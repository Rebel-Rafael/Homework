1.理论

1.1.外部样式表怎么引入？
	使用<link rel="stylesheet" href="">

1.2.在CSS的标准盒模型中，元素的总宽度 = width + padding-left + padding-right + border-left + border-right + margin-left + margin-right

1.3.CSS的什么伪类用于选择鼠标悬停时的元素状态？
	:hover

1.4.CSS选择器的优先级按顺序从高到低为？
	!important > 内联样式 > ID选择器 > 类选择器 > 伪类 >  伪元素

1.5.在CSS中，使用什么可以使某个样式规则具有更高的优先级，覆盖普通规则？
	!important